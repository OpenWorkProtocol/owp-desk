# Agent setup

Read `skills/owp-desk/SKILL.md` completely before working as a freight desk session.
That file is the canonical owp-desk skill used by Codex, Claude Code, and Pi.
Run `npm install`, then `npm test`. `npm run demo`
starts a disposable synthetic day; real deployments use the configured OWP
surface and authority policies before committing money, capacity, or liability.
